<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta name="description" content="FAQ de l'environnement - CLIMAT ÉNERGIE BIODIVERSITÉ INJUSTICE CONQÉQUENCES" />
        <link rel="icon" href="https://emojipedia-us.s3.dualstack.us-west-1.amazonaws.com/thumbs/120/samsung/306/green-book_1f4d7.png">
        <title>{% block title %}FAQ de l'environnement{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}
        <link href="https://api.fontshare.com/css?f[]=panchang@400,500,600,700&f[]=supreme@400,500,700,800&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="{{ asset('style.css') }}">
        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
    <header>
        <nav class="d-flex flex-between">
            <a href="{{ path('home') }}">ACCUEIL</a>
            <div class="theme-links">
                {% for theme in themes.access %}
                    <a href="{{ path(theme.title) }}">{{ theme.title }}</a>
                {% endfor %}
            </div>
        </nav>
    </header>
    <div class="body-content-wrapper {% if app.request.attributes.get('_route') == 'home' %}home-content-wrapper{% endif %}">
        {% block body %}{% endblock %}
    </div>
    <footer>
        <p>made with love by <a href="https://github.com/alexandre-mace">@alexandre-mace</a></p>
    </footer>
    </body>
</html>
